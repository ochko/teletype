#!/usr/bin/env ruby

require_relative '../lib/teletype'

text = IO.read File.join(File.dirname(__FILE__), "../", 'exercise', 'ruby', 'yield.rb')

practice = Teletype::Practice.new(text)
practice.start
